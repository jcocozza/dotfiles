set equalprg=prettier
set formatprg=prettier

" see https://github.com/preservim/vim-markdown
" this isn't in the my dotfiles right now
" but might want to add back
" let g:vim_markdown_frontmatter = 1
" let g:vim_markdown_math = 1
